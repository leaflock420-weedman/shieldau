services:
  - type: web
    name: shieldau
    runtime: static
    buildCommand: echo "Static site — no build"
    staticPublishPath: docs
    routes:
      - type: rewrite
        source: /*
        destination: /index.html