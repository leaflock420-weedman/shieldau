services:
  - type: web
    name: shieldau
    runtime: static
    buildCommand: echo "Static site — no build"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html